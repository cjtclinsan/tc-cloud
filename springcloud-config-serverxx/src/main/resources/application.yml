server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jieblog/config-repo-demo-.git

eureka:
  client:
    service-url:
      defaultZone: http://eureka.didispace.com/eureka/

logging:
  config: classpath:logback.xml



